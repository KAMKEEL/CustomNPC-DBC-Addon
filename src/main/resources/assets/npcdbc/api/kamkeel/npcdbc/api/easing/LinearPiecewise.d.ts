/**
 * Generated from Java file for CustomNPC+ Minecraft Mod 1.7.10
 * Package: kamkeel.npcdbc.api.easing
 */

export class LinearPiecewise {
    IllegalArgumentException(x: import('./"Stops must be paired:')."Stops must be paired:): import('./throw new').throw new;
    IllegalArgumentException([0: import('./"x values must be in')."x values must be in, x: import('./1]: " +').1]: " +): import('./throw new').throw new;
    IllegalArgumentException(prevX: "x values must be strictly increasing: " + x + " <= " +): import('./throw new').throw new;
    if(0: import('./temp_xStops[0] >').temp_xStops[0] >): import('./add start if missing').add start if missing;
    if(1.0: temp_xStops[temp_Segments - 1] <): import('./add end if missing').add end if missing;
    ease(time: number): number;
    getXStops(): number[];
    getYValues(): number[];
    getSegmentCount(): number;
    xStops: number[];
    yValues: number[];
    segments: number;
}

